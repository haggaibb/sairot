# ימי סיירות - מבדקים (Sairot - Scouting Days Tests)

A Flutter application for managing and tracking scouting/reconnaissance day assessments and tests.

## 📱 Overview

This application is designed for instructors and administrators to manage scouting day events, track participant performance, and generate reports. The app supports both instructor and admin workflows.

## 🏗️ Project Structure

```
sairot/
├── lib/
│   ├── admin/              # Admin-specific functionality
│   ├── models/             # Data models
│   ├── pages/              # UI pages
│   ├── widgets/            # Reusable widgets
│   └── main.dart          # Main app entry point
├── android/                # Android platform files
├── images/                 # App assets and logos
└── pubspec.yaml           # Dependencies
```

## 🚀 Getting Started

### Prerequisites

- Flutter SDK (3.6.1 or higher)
- Dart SDK
- Android Studio / Xcode (for mobile development)
- Firebase project setup

### Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/haggaibb/sairot.git
   cd sairot
   ```

2. **Install dependencies**
   ```bash
   flutter pub get
   ```

3. **Configure Firebase**
   - Download `google-services.json` from Firebase Console
   - Place it in `android/app/google-services.json`
   - ⚠️ **Never commit this file to git** (already in .gitignore)

4. **Configure Android**
   - Ensure `local.properties` is set up (auto-generated by Android Studio)
   - ⚠️ **Never commit this file to git** (already in .gitignore)

5. **Run the app**
   ```bash
   flutter run
   ```

## 🔐 Security & Sensitive Data

### Files Excluded from Git

The following sensitive files are excluded via `.gitignore`:

- `android/app/google-services.json` - Firebase configuration
- `android/local.properties` - Android SDK paths
- `*.keystore`, `*.jks` - Signing keys
- `.env*` - Environment variables
- `*.hive`, `*.lock` - Local database files
- `*.apk`, `*.aab` - Build artifacts

### Setting Up Firebase

1. Create a Firebase project at [Firebase Console](https://console.firebase.google.com/)
2. Add Android app with package name: `com.example.sairot`
3. Download `google-services.json`
4. Place it in `android/app/google-services.json`
5. Configure Firestore, Authentication, and Storage as needed

### Setting Up for New Developers

1. Clone the repository
2. Copy `google-services.json` from team member or Firebase Console
3. Run `flutter pub get`
4. Build and run the app

## 📦 Dependencies

Key dependencies include:

- **State Management**: `get: ^4.6.6` (GetX)
- **Firebase**: `firebase_core`, `cloud_firestore`, `firebase_storage`
- **Local Storage**: `hive`, `hive_flutter`
- **Charts**: `fl_chart: ^0.70.2`
- **Tables**: `pluto_grid: ^8.0.0`
- **Localization**: `flutter_localization: ^0.2.3`

See `pubspec.yaml` for complete list.

## 🏃 Running the App

### Development

```bash
# Run on connected device/emulator
flutter run

# Run in release mode
flutter run --release

# Build APK
flutter build apk

# Build App Bundle
flutter build appbundle
```

### Platform-Specific

- **Android**: Minimum SDK 23, Target SDK 36
- **Web**: Supported (configure Firebase for web separately)

## 📱 App Features

### Instructor Features
- Login with ID
- Track event progress (Meshulash, Alonka, Bur, Sakim)
- Record participant grades and comments
- View participant performance
- Generate reports

### Admin Features
- Admin login
- Live event monitoring
- Event reports and analytics
- Access to all instructor data
- Grade distribution charts

## 🔧 Development Guidelines

### Code Structure

- **Controllers**: Use GetX controllers for state management
- **Models**: Data models with JSON serialization
- **Pages**: UI pages organized by feature
- **Widgets**: Reusable UI components

### Naming Conventions

- Files: `snake_case.dart`
- Classes: `PascalCase`
- Variables: `camelCase`
- Constants: `UPPER_SNAKE_CASE`

### Git Workflow

- Main branch: `main` (production)
- Development branch: `dev-1.2`
- Feature branches: `feature/feature-name`
- Commit messages: Use descriptive messages in Hebrew or English

## 🧪 Testing

```bash
# Run tests
flutter test

# Run with coverage
flutter test --coverage
```

## 📝 Building for Production

### Android

1. **Generate signing key** (if not exists)
   ```bash
   keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
   ```

2. **Configure signing** in `android/app/build.gradle`

3. **Build release APK**
   ```bash
   flutter build apk --release
   ```

4. **Build App Bundle** (for Play Store)
   ```bash
   flutter build appbundle --release
   ```

## 🐛 Troubleshooting

### Common Issues

1. **Firebase not initialized**
   - Ensure `google-services.json` is in `android/app/`
   - Run `flutter clean` and rebuild

2. **Build errors**
   - Run `flutter clean`
   - Delete `build/` folder
   - Run `flutter pub get`
   - Rebuild

3. **Dependencies conflicts**
   - Run `flutter pub upgrade`
   - Check `pubspec.lock` for version conflicts

## 📄 License

[Specify your license here]

## 👥 Contributors

[Add contributor information]

## 📞 Support

For issues and questions, please open an issue on GitHub.

---

**⚠️ Important Security Notes:**

- Never commit `google-services.json` or `local.properties`
- Keep signing keys secure and never commit them
- Use environment variables for API keys in production
- Regularly update dependencies for security patches
